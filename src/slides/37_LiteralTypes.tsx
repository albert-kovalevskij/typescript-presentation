import { Slide, Text, Heading } from "spectacle";

const LiteralTypes = () => (
  <Slide backgroundColor="white">
    <Heading color="black">Literal Types</Heading>
    <Text fontSize="30px">
      In addition to the general types <code>string</code> and{" "}
      <code>number</code>, we can refer to <em>specific</em> strings and numbers
      in type positions.
    </Text>
  </Slide>
);

export default LiteralTypes;
